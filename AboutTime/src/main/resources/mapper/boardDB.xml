<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.about_time.community.mapper.BoardMapper">
	
	<insert id="insertBoard" parameterType="com.about_time.community.vo.Board">
		insert into Board(title,writer,category,contents,university,regDate,updateDate) 
		values (#{title},#{writer},#{category},#{contents},#{university},now(),now())
	</insert>
	
</mapper>